import React from 'react';

import styles from './styles.module.css';

function AppBar() {
    return (
        <div className={styles.appbar}>
            {/* <input type="text" name="searchField" placeholder="Хайх утгаа оруулна уу..." className={styles.search_field} /> */}
        </div>
    )
}

export default AppBar;